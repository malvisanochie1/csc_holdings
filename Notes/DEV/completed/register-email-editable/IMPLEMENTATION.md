# Register Email Editable Implementation

## Date
2025-11-17

## Summary
Removed the `disabled` and `readOnly` attributes from the `/register` email input so users can type their address when creating an account, then recompiled the app with `yarn build` to ensure no regressions.
